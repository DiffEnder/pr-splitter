diff --git a/lib/features/chat/chatThunks.ts b/lib/features/chat/chatThunks.ts
index fb766b16..6091cdda 100644
--- a/lib/features/chat/chatThunks.ts
+++ b/lib/features/chat/chatThunks.ts
@@ -577,7 +577,7 @@ export const addLinesToChat =
     if (!state.chat.selectedChatId) return
     const { baseSha, headSha } = selectHeadAndBaseSha(getState())
     const currentUserMessageDraftId = getUserMessageDraftId({ chatId: state.chat.selectedChatId })
-    dispatch(uiChanged({ isOpen: true }))
+    dispatch(uiChanged({ isOpen: true, selectedTab: 'chat' }))
     dispatch(userMessageFocused({ messageId: currentUserMessageDraftId }))
 
     if (!isNumber(editorLineIndex)) {
diff --git a/lib/features/chat/components/AddToChatContainer.tsx b/lib/features/chat/components/AddToChatContainer.tsx
index d1f806ba..574b83ff 100644
--- a/lib/features/chat/components/AddToChatContainer.tsx
+++ b/lib/features/chat/components/AddToChatContainer.tsx
@@ -20,7 +20,7 @@ export const AddToChatContainer = React.memo(
     const dispatch = useAppDispatch()
     const onClick = useCallback(() => {
       dispatch(addSelectionToChat())
-      dispatch(uiChanged({ isOpen: true, addSelectionToChatLocation: null }))
+      dispatch(uiChanged({ isOpen: true, selectedTab: 'chat', addSelectionToChatLocation: null }))
     }, [dispatch])
     const isVisible = useAppSelector(
       (state) =>
diff --git a/lib/features/diffTree/diffTreeSelectors.ts b/lib/features/diffTree/diffTreeSelectors.ts
index 8f97a443..260d5be1 100644
--- a/lib/features/diffTree/diffTreeSelectors.ts
+++ b/lib/features/diffTree/diffTreeSelectors.ts
@@ -73,9 +73,12 @@ export const selectShouldShowDiffTreeToggleButton = createSelector(
   },
 )
 
-export const selectDiffTreeWidth = (state: RootState) => {
-  if (state.diffTree.view === 'expanded') return state.diffTree.containerWidth
-  return 0
-}
+export const selectDiffTreeWidth = createSelector(
+  selectShouldShowDiffTree,
+  (state: RootState) => state.diffTree.containerWidth,
+  (shouldShow, containerWidth) => {
+    return shouldShow ? containerWidth : 0
+  },
+)
 
 export const selectIsDiffTreeResizing = (state: RootState) => state.diffTree.isResizing
diff --git a/lib/features/pullRequest/components/PullRequestContainer.tsx b/lib/features/pullRequest/components/PullRequestContainer.tsx
index 9ba1c20d..e96f815f 100644
--- a/lib/features/pullRequest/components/PullRequestContainer.tsx
+++ b/lib/features/pullRequest/components/PullRequestContainer.tsx
@@ -154,7 +154,7 @@ export const PullRequestContainer = () => {
               className={styles.filesContainer}
               style={{
                 width: `calc(100% - (${chatWidth + diffTreeWidth + 64}px))`,
-                marginLeft: diffTreeWidth + 32,
+                marginLeft: showTree ? diffTreeWidth + 32 : 32,
               }}
             >
               {gitHubFiles.map((file) => (
